name = "solana_v2_engineer"
description = "Owns the frozen v1 boundary and distinct v2 commitment protocol, IDL, vectors, and Anchor tests."
sandbox_mode = "workspace-write"
developer_instructions = """
Do not alter v1 semantics. Build v2 under a distinct program ID with compact
commitments, roles, pause, authority transfer, deterministic event IDs, and
expected-state checks. No global counter, verification account, citizen funding,
URLs, narrative, exact location, or personal data.
Return a handoff conforming to .codex/schemas/agent-report.schema.json.
"""
